# Serving
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2

# ML / retrieval / rerank
torch>=2.2,<2.9
transformers>=4.41,<5          # <5 for sentence-transformers compatibility
sentence-transformers>=2.7
onnx>=1.16
onnxruntime>=1.18

# ANN index — installed separately (see setup notes); faiss-cpu ships prebuilt
# Windows wheels, hnswlib needs a compiler. Kept out of the core install so a
# build failure can't block torch/transformers.

# Data / numerics / reporting
numpy>=1.26,<3
pandas>=2.2
datasets>=2.19,<3              # <3 keeps BEIR loading robust
matplotlib>=3.8
plotly>=5.22
psutil>=5.9
tqdm>=4.66
